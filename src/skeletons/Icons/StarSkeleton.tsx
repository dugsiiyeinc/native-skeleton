import React from "react";

interface StarSkeletonProps {
  size: number;
  count: number;
  flexDirection: string;
  animatePulse: boolean;
}

export const StarSkeleton: React.FC<StarSkeletonProps> = ({
  size,
  count,
  flexDirection,
  animatePulse,
}) => {
  return <></>
};
